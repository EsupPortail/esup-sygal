<?php

use Application\Entity\Db\These;
use Application\View\Renderer\PhpRenderer;
use Zend\Form\Form;

/**
 * @var        $this         PhpRenderer
 * @var These  $these
 * @var Form   $form
 * @var string $title
 * @var bool   $detournement Indique s'il s'agit d'un détournement de requête pour saisie du persopass
 * @var string $emailBdD
 *
 * @see \Doctorant\Controller\DoctorantController::modifierEmailContactAction()
 */

$appName = $this->appInfos()->nom;
?>

<h1 class="page-header"><?php echo $title ?></h1>

<?php echo $this->messenger()->addMessagesFromFlashMessengerWithNoNamespace(); ?>

<div class="container">
    <div class="col-md-8">
        <p class="lead">
            Merci de saisir une adresse mail de contact.
            Cette adresse sera utilisée par <?php echo $appName ?> pour vous communiquer les informations importantes associées à votre
            thèse.
        </p>
        <p class="alert alert-info">
            <strong>NB: </strong>
            L'adresse mail de contact que vous saisissez ici doit être une adresse durable (autre que vos adresses mails
            fournies par votre établissement).
        </p>
    </div>
</div>

<div class="container">
    <div class="col-md-4 col-md-offset-2">
        <?php
        $fcg = $this->formControlGroup();
        echo $this->form()->openTag($form->prepare()->setAttribute('class', 'mail-confirmation'));
        echo $fcg($form->get('id'));
        echo $fcg($form->get('idIndividu'));
        echo $fcg($form->get('individu'));
        echo $fcg($form->get('email'));
        echo $fcg($form->get('enregistrer'));
        echo $this->form()->closeTag();
        ?>
    </div>
</div>
