<?php

/**
 * @see \Formation\Controller\SessionController::indexAction()
 * @var Session[] $sessions
 */

use Formation\Entity\Db\Inscription;
use Formation\Entity\Db\Seance;
use Formation\Entity\Db\Session;


$canAfficher = true;
$canModifier = true;
$canHistoriser= true;
?>

<h1> Index des sessions </h1>

<a
    <?php /** @see \Formation\Controller\SessionController::ajouterAction() */ ?>
    href="<?php echo $this->url('formation/session/ajouter', ['module' => 2], [], true); ?>"
    class="btn btn-primary action">
    <span class="glyphicon glyphicon-plus"></span>
    Ajouter une session
</a>


<table class="table table-condensed">
    <thead>
    <tr>
<!--        <th>Identifiant</th>-->
        <th>Module</th>
        <th>Site</th>
        <th>Responsable</th>
        <th>Modalité</th>
        <th>Type</th>
        <th>État</th>
        <th>Séances</th>
        <th>Inscriptions</th>
        <th>Action</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($sessions as $session) : ?>
        <tr>
<!--            <td> --><?php //echo $session->getId(); ?><!-- </td>-->
            <td>
<!--                --><?php //echo $session->getFormation()->getId(); ?>
                <?php echo $session->getModule()->getLibelle(); ?> <br/>
                Session #<?php echo $session->getIndex(); ?> <br/>
            </td>
            <td> <?php echo $this->site($session); ?> </td>
            <td>
                <?php if ($session->getResponsable() !== null) : ?>
                    <?php echo $session->getResponsable()->getNomComplet(); ?>
                <?php else : ?>
                    Aucun responsable
                <?php endif; ?>
            </td>
            <td> <?php echo $this->modalite($session); ?> </td>
            <td> <?php echo $this->type($session); ?> </td>
            <td>
                <?php echo ($session->getEtat())?:"Non Précisé"; ?>
            </td>
            <td> <!-- Seances ----------------------------------------------------------------------------------------->
                <?php echo count($session->getSeances()); ?>
                séance<?php if (count($session->getSeances())>1) echo "s"; ?>
                (<?php echo $session->getDuree(); ?> h.)
                <a href="<?php echo $this->url('formation/seance/ajouter', ['session' => $session->getId()], [], true); ?>"
                   class="ajax-modal" data-event="modification"
                   title="Ajout d'une séance pour la session de formation #<?php echo $session->getId(); ?>"
                   data-toggle="tooltip" data-html="true">
                    <span class="glyphicon glyphicon-plus"></span>
                </a>

                    <ul>
                        <?php /** @var Seance $seance */ ?>
                        <?php foreach ($session->getSeances() as $seance) : ?>
                        <li>
                            <?php echo $seance->getDebut()->format('d/m/Y'); ?>
                            <?php echo $seance->getDebut()->format('H:i') ; ?>
                            &rightarrow;
                            <?php echo $seance->getFin()->format('H:i') ; ?>
                            -
                            <?php echo $seance->getLieu() ; ?>
                            <?php /** @see \Formation\Controller\SeanceController::modifierAction() */?>
                            <a href="<?php echo $this->url('formation/seance/modifier',['seance' => $seance->getId()], [], true); ?>"
                               class="ajax-modal" data-event="modification"
                               title="Modification de la séance #<?php echo $seance->getId(); ?>" data-toggle="tooltip" data-html="true">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </a>
                            <?php if ($seance->estNonHistorise()) : ?>
                                <a href="<?php echo $this->url('formation/seance/historiser',['seance' => $seance->getId()], ['query' => ['retour' => $this->url('formation/session',[],[],true)]], true); ?>"
                                   title="Historisation de la séance #<?php echo $seance->getId(); ?>" data-toggle="tooltip" data-html="true">
                                    <span class="glyphicon glyphicon-trash"></span>
                                </a>
                            <?php else : ?>
                                <a href="<?php echo $this->url('formation/seance/restaurer',['seance' => $seance->getId()], ['query' => ['retour' => $this->url('formation/session',[],[],true)]], true); ?>"
                                   title="Restauration de la séance #<?php echo $seance->getId(); ?>" data-toggle="tooltip" data-html="true">
                                    <span class="glyphicon glyphicon-leaf"></span>
                                </a>
                            <?php endif; ?>
                            <?php /** @see \Formation\Controller\SeanceController::supprimerAction() */?>
                            <a href="<?php echo $this->url('formation/seance/supprimer',['seance' => $seance->getId()], [], true); ?>"
                               class="ajax-modal" data-event="modification"
                               title="Suppression de la séance #<?php echo $seance->getId(); ?>" data-toggle="tooltip" data-html="true">
                                <span class="glyphicon glyphicon-remove"></span>
                            </a>
                        </li>
                        <?php endforeach; ?>
                    </ul>
            </td>
            <td> <!-- Inscriptions ------------------------------------------------------------------------------------>
                Liste principale : <?php echo count($session->getInscriptionsByListe(Inscription::LISTE_PRINCIPALE)); ?>/<?php echo $session->getTailleListePrincipale(); ?> <br/>
                Liste secondaire : <?php echo count($session->getInscriptionsByListe(Inscription::LISTE_COMPLEMENTAIRE)); ?>/<?php echo $session->getTailleListeComplementaire(); ?> <br/>
                Hors liste : <?php echo count($session->getInscriptionsByListe(null)); ?><br/>
            </td>
            <td>
                <?php if ($canAfficher) : ?>
                    <?php /** @see \Formation\Controller\SessionController::afficherAction() */ ?>
                    <a href="<?php echo $this->url('formation/session/afficher', ['session' => $session->getId()], [], true); ?>">
                        <span class="glyphicon glyphicon-eye-open"></span>
                    </a>
                <?php endif; ?>
                <?php if ($canModifier) : ?>
                    <?php /** @see \Formation\Controller\SessionController::modifierAction() */ ?>
                    <a href="<?php echo $this->url('formation/session/modifier', ['session' => $session->getId()], [], true); ?>"
                       class="ajax-modal" data-event="modification"
                    >
                        <span class="glyphicon glyphicon-pencil"></span>
                    </a>
                <?php endif; ?>

                <?php if ($canHistoriser) : ?>
                    <?php if ($session->estNonHistorise()) : ?>
                        <?php /** @see \Formation\Controller\SessionController::historiserAction() */ ?>
                        <a href="<?php echo $this->url('formation/session/historiser', ['session' => $session->getId()], [], true); ?>"
                        >
                            <span class="glyphicon glyphicon-trash"></span>
                        </a>
                    <?php else : ?>
                        <?php /** @see \Formation\Controller\SessionController::restaurerAction() */ ?>
                        <a href="<?php echo $this->url('formation/session/restaurer', ['session' => $session->getId()], [], true); ?>"
                        >
                            <span class="glyphicon glyphicon-leaf"></span>
                        </a>
                    <?php endif; ?>
                <?php endif; ?>
            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<script>
    $(function() {
        $("body").on("modification", function (event) {
            event.div.modal('hide');
            window.location.reload();
        });
    });
</script>